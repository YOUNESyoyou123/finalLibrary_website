/*etudconst express = require("express");
const mongoose = require("mongoose");
const cors = require("cors");

const app = express();
const Etudiant = require("./models/model");


// Set up CORS middleware
app.use(cors());
app.use(express.json());*/

function getimage() { 
    fetch("http://localhost:3000/get-image" , {
        method : "GET",
    }.then((re)=>resizeBy.json().then((data)=>{
        console.log(data)
        setAllImage(data.data)
    }))
    
    )
}